# Architecture

The detailed architecture guide for this repository lives at [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md).

This root file exists so organization-wide architecture sweeps and GitHub file lookup can find an `Architecture.md` entrypoint without duplicating the canonical guide maintained under `docs/`.
